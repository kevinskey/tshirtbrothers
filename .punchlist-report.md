# DTF store punch list — implementation report

## A. Admin settings endpoints + card

- `server/routes/gangsheetStore.js:53` — new export `tierPromises(settings)`, built from a new local `fmt()` helper (`:40`) that formats a Postgres `TIME` string ('HH:MM:SS') to '11:00 AM'/'1:30 PM'. `TIER_PROMISES` (`:25`) kept exported as the static fallback.
- `server/routes/gangsheetStore.js:124` — `/config`'s `promises` field now `tierPromises(s)` instead of the static `TIER_PROMISES`.
- `server/routes/gangsheetStore.js:303` (adaptation, not in brief) — the Stripe line-item `description` for the checkout session also switched from static `TIER_PROMISES[tier]` to `tierPromises(s)[tier]`, since `s` was already loaded in that handler — keeps the Stripe checkout page, `/config`, and emails all showing the same live cutoff text instead of leaving one place stale.
- `server/routes/gangsheetStore.js:111` — `priceCents`'s tier-rate check changed from `if (!rate)` to `if (rate == null)` so a configured `0` rate no longer throws.
- `server/routes/gangsheetStore.js:336` — `GET /admin/settings` (adminGuard) returns the full `gang_sheet_store_settings` row via `loadSettings()`.
- `server/routes/gangsheetStore.js:344-393` — `PATCH /admin/settings` (adminGuard): whitelist array `SETTINGS_FIELDS` of `[column, validator]` pairs (column names are never interpolated from input — only from this fixed array); builds `SET` clauses + parameterized values for whichever fields are present in the body; cross-validates `min_ft`/`max_ft` (fetching current settings when only one of the two is in the PATCH) with 400 `'min_ft must be ≤ max_ft'`; 400 `'Invalid value for <field>'` per bad field; 400 `'No valid fields to update'` if the body has none of the whitelisted keys; sets `updated_at = now()`; returns the updated row.
- `server/services/email.js:903` — new `gangSheetTurnaround(tier)` helper: dynamic `await import('../routes/gangsheetStore.js')` (function-body-only, matching the established cycle-avoidance pattern used elsewhere in this file / `server/routes/mockups.js`) to call `loadSettings()` + `tierPromises()`; falls back to the static `TIER_PROMISES` on any failure (settings row missing, DB hiccup, etc.) so an email never fails to send over this.
  - `sendGangSheetPaidToCustomer` (`:931`) and `sendGangSheetPaidToAdmin` (`:994`) now call it for their turnaround/tier line.
  - **Adaptation**: the brief described "the two paid senders + ready sender" as using static `TIER_PROMISES`. In the actual code, `sendGangSheetReadyToCustomer` never referenced `TIER_PROMISES` at all (its body only shows Order/Size/Total, no turnaround line) — so there was nothing to switch there. Left unchanged; noting it rather than inventing a turnaround line that wasn't part of its original design.
- `client/src/pages/AdminDtfOrdersPage.tsx:196-441` — new `SettingsCard` component: collapsible (`ChevronDown`/`ChevronUp`), lazy-loads `/admin/settings` only while open, inputs for the three rates (dollars in the UI, converted to/from cents via `dollarsToCents`/`centsToDollarStr`), two `type="time"` cutoffs (`toHHMM` strips the DB's `:SS`), min/max ft, shipping (dollars), three active toggles. Save button PATCHes, toasts success/error, invalidates both `dtf-admin-settings` and `dtf-config` (the second so DtfStorePage sees the new copy/prices on its next load). Rendered at `:491`, above the queue, inside the already-admin-gated page.

## B. Hardening smalls

1. `server/routes/gangsheetStore.js:183-191` — `/upload` now loads settings and rejects with 400 `` `Your file is ${ft} ft long — our max sheet is ${max_ft} ft. Split it into two sheets.` `` before ever uploading to Spaces, when `dims.height > settings.max_ft * 3600 * 1.02`.
2. `client/src/pages/DtfStorePage.tsx:227-241` — client mirrors the same check: if the raw needed length exceeds `config.max_ft`, `setUploadError` with the identical split-it message instead of computing a bump suggestion.
3. `client/src/pages/DtfStorePage.tsx:64,310,620` (`EMAIL_RE`, `emailValid`, inline message under the email field) + `server/routes/gangsheetStore.js:213,219-221` (`EMAIL_RE`, 400 `'Enter a valid email address'` before any other checkout validation runs).
4. `client/src/pages/DtfStorePage.tsx:59-60` (`UPLOAD_STASH_KEY`, 2h max age), `:153-175` (restore-on-mount effect, validates shape before trusting it), `:219-225` (stash write right after a successful upload, including `fileName`), `:293` (`sessionStorage.removeItem` immediately before `window.location.href = url`). Uploaded-state UI (`:514-524`) now also shows `uploadedFile.fileName` when present, satisfying "renders the same uploaded-state UI with the original file name" for a restored upload.
5. `client/src/pages/DtfStorePage.tsx:181-193` — `prevLengthRef` tracks the previous *non-null* length; the null→config-default population is recorded but never fires the event; every subsequent user-driven change fires `dtf-length-set { ft }` 800ms after the value settles (standard debounce-with-cleanup, matching the `useEffect`+`setTimeout` pattern already used in `InstantQuotePage.tsx`).
6. `server/routes/gangsheetStore.js:408-447` — `ALLOWED_FROM` keyed by *target* status per the brief's map. **Adaptation**: `'paid'` is not a key in `ALLOWED_FROM` (nothing transitions to it through this endpoint — only the Stripe webhook sets `paid`), so it's no longer a legal PATCH target at all, replacing the old `NEXT_STATUSES` array that had included it. Implemented as a single `UPDATE ... WHERE id = $2 AND status = ANY($3)` (atomic, no TOCTOU); on no-match, a follow-up `SELECT` distinguishes 404 (no such order) from 400 `` `Can't go from ${current} to ${status}` ``.
7. `server/routes/gangsheetStore.js:326` — `stripe.checkout.sessions.create(..., { idempotencyKey: \`gso-${orderId}\` })`.
8. `server/routes/gangsheetStore.js:222-239` — name ≤120, email ≤254, note ≤2000, each `ship_address` string field ≤120 → 400 `'That field is too long'`.
9. `server/services/email.js:917-925` (`parseShipAddress`, object-or-JSON-string guard) + `:995,1002` (`sendGangSheetPaidToAdmin` adds a `detailRow('Ship to', escapeHtml(...))` when `order.delivery === 'ship'` and the address parses to something truthy).
10. `client/src/pages/AdminDtfOrdersPage.tsx:465` (`mutatingId` state), `:480` (`onSettled` clears it), `:483-486` (`handleAdvance` sets it before `mutate`), `:612,622` (`disabled={mutatingId === order.id}` on both the advance and cancel buttons, replacing the shared `advance.isPending`).
11. `server/migrations/gang_sheet_store_v2.sql` — `ALTER TABLE gang_sheet_orders ADD COLUMN IF NOT EXISTS attested BOOLEAN NOT NULL DEFAULT FALSE;`. `server/routes/gangsheetStore.js:218,285-289` — `/checkout` destructures `attested` and stores `attested === true` (coerced, so non-boolean garbage in the body can't write anything but `false`). `client/src/pages/DtfStorePage.tsx:44,280` — `CheckoutBody.attested`, sent as `attested: agree` (the existing transparency-checkbox state, which is already required truthy for `canCheckout` to be true).
12. `server/services/scheduler.js:12-14` (imports `DeleteObjectCommand`, `getSpacesClient`/`SPACES_BUCKET`), `:56-77` (`purgeAbandonedGangSheetCheckouts`: selects `pending_payment` rows older than 24h, best-effort `DeleteObjectCommand` per `file_key` with per-object error logging, then bulk `DELETE ... WHERE id = ANY(...)`, logs the purged count), `:92-95` (wired into `startScheduler()` at `cron.schedule('30 6 * * *', ...)`, offset 30m after the existing payout job), `:96` (startup log line updated to mention the new job).

## Verification

`node --check` on every touched server file — all clean:

```
$ node --check server/routes/gangsheetStore.js && echo OK
OK
$ node --check server/services/email.js && echo OK
OK
$ node --check server/services/scheduler.js && echo OK
OK
```

Client TS (`client/src/pages/DtfStorePage.tsx`, `client/src/pages/AdminDtfOrdersPage.tsx`) reviewed by hand against strict + `noUncheckedIndexedAccess` + `noUnusedLocals`/`noUnusedParameters` (no local `tsc` available in this worktree — brief says not to `npm install`). No new `Record`/index-signature indexing was introduced beyond the file's existing `Record<TierKey, ...>` literal-union pattern (not subject to `noUncheckedIndexedAccess`); all new state/refs are fully typed; no unused imports, locals, or params found in the diff. Bracket-balance sanity-checked both files programmatically as a mechanical backstop.

## Explicitly not touched

Queue thumbnails, Phase 2 builder, quote/sale flows — per brief. `DtfSuccessPage.tsx` — no punch-list item required a change there; its existing `SUCCESS_STASH_KEY` stash is unrelated to and doesn't collide with the new `UPLOAD_STASH_KEY`.
